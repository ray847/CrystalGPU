# TODO List

* Implement concurrent prodecure running in device.
* Add more language features to `glan`:
  * readonly
  * structs
  * vectors & matricies
  * swizzeling
  * if-else statements
  * switch statements
  * loop statements
  * builtin types
  * richer statements
* Stricter compile time constraints for `glan` so that no shader compilation errors for the wgsl code afterwards.
* Pipeline and shader (SPIRV interpretation) caching
* Write a decent example for README QuickStart.